- [x] Train ml_model.py on the data of Training.csv
- [x] Update model pipeline to use Training.csv features + prognosis labels (instead of synthetic subsets from DISEASE_DB)

- [ ] Ensure predictor.py loads trained model/artifacts and can predict from user symptom strings to CSV symptom columns
- [ ] Add evaluation hooks for graphs (optional) using CSV test split
- [ ] Run smoke test: `python interface.py` and `python app.py`

